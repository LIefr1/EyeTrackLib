import torch
import torch.nn as nn


class FaceMeshModel(nn.Module):
    def __init__(self, num_classes=4, device="cuda"):
        super(FaceMeshModel, self).__init__()
        self.device = device

        def forward(self, x):
            return x
